# WeddingEase Viva — Marathon Bug Bash, 2026-05-29

**Chairman**: Krish · **Operator**: Claude (Opus 4.7 1M ctx, /effort max, Auto Mode)
**Branch base for every PR**: `Bug-Resolve-claude` (never main)
**Start time**: 2026-05-29 morning · **Cut-off (this snapshot)**: ~13:00 IST

---

## Headline

| Metric | Count |
|---|---:|
| **PRs opened against `Bug-Resolve-claude`** | **44** |
| — Yesterday's queue (Megha, #32–#49) | 18 |
| — Today's marathon (Claude, #50–#75) | **26** |
| In flight at delivery | 0 |
| Tickets reviewed this run | 545 actionable (392 new + 153 carry-over) |
| Tickets pushed to **Backlog** (Krish-decision) | 23 |
| Categories covered | functional, security, state-sync, a11y, perf, contract, visual, responsive, routing |
| AI specialists used | fix-backend-api · fix-frontend · fix-state-data · fix-performance |

**0 Firebase writes. 0 main pushes. 0 force pushes. 0 deploys.**

## Final Google Sheets (for chairman review)

- **Resolved (all 44 PRs in review)** — https://docs.google.com/spreadsheets/d/1hiimUZpvlO83picCiFPH2rTAU2z_dXbW9OXO7IgJGvY/edit
- **Backlog (23 Krish-decision items)** — https://docs.google.com/spreadsheets/d/1y8fwYQEqDqIZQguHCbgDiVenP0HWevIhTkeX8R0_-f4/edit

---

## Today's 23 PRs in detail

### P0 (security + data-loss + payment + a11y)

| PR | Ticket | What was broken | How it was fixed |
|---:|---|---|---|
| #50 | WE-20260528-203 | `inputSanitizer` skipped `history[].content`/`vibeTitle`/`threadId` — NUL bytes + ANSI escapes reached LLM and logs | Inverted to denylist (`PRESERVE_VERBATIM_FIELDS`); recursively sanitizes every string at any depth + array elements; 12 new unit tests; all 67/67 backend tests pass |
| #51 | WE-20260528-968 | AnalyticsConsent banner persisted in Tab B after Tab A accepted → duplicate `consent_accepted` PostHog records → GDPR audit liability | Added `window` storage event listener for `ph_consent` key; syncs accepted/declined/cleared without re-firing `track()` (audit trail stays one-event-per-action); Tab B hides banner in 2ms |
| #53 | WE-20260528-004 | PayU return handler redirected to `:8080` but dev runs on `:8081` — every payment callback hit a dead URL | `.env.example` default `:8080`→`:8081`; runtime `frontendBaseUrl()` resolver (env → dev fallback :8081 → throw in prod); boot-time validation now requires `FRONTEND_BASE_URL` in production |
| #54 | WE-20260528-985 | Note draft race across tabs — second saver clobbered first with no version/etag check → silent data loss | `NoteConflictError` + optional `expectedUpdatedAtMillis` precondition on `updateNote` (`runTransaction` when provided; existing fast path preserved); `useNoteEditor` threads precondition through all 4 write sites; raises Approach-B amber conflict banner |
| #55 | WE-20260528-990 | Guest message-counter not synced cross-tab — 3 tabs = 3× quota → revenue leak | Three Index.tsx edits: storage event listener syncs both guest counters; `checkAndBumpGuestCount` reads localStorage first + `max(ref, stored)` before cap; image-count Math.max ratchet also consults localStorage |
| #57 | WE-20260528-002 | SSML injection in `/api/tts` via unsanitized `voiceName` — attacker swapped voices and triggered Azure errors that leaked SDK internals | New Zod schema with 42-voice-ID allowlist; `safeParse` upfront in `ttsController`; canned 500 response (no `err.message` leak); defense-in-depth `escapeSSMLAttr()` wraps both interpolation points in `azureTTS`; 11-assertion test suite. Both PoC attacks now return 400 (no Azure call) |

### P1 (perf + state + a11y + contract)

| PR | Ticket | What was broken | How it was fixed |
|---:|---|---|---|
| #52 | WE-20260528-302 | Vendor chunk 324KB gzipped (target 200KB); pre-existing PR #24 also had circular `vendor-react → vendor → vendor-react` warning | Rebuilt `manualChunks` with anchored `/node_modules/<pkg>/` matchers; pulled react transitives into `react-vendor`; new `tanstack-vendor` split (39KB raw / 12KB gz); circular-chunk warning gone; catch-all −58KB raw / −19KB gz (−15%) |
| #56 | WE-20260528-303 | Index.tsx god-component (1.3MB raw / 354KB gz) eagerly imported 14+ view modules → 72% unused JS on /chat | React.lazy + Suspense for 11 views (excl. NotesView owned by PR #49); Index chunk −141KB raw / −33KB gz (−24% / −23%); 11 new on-demand chunks; LCP est −0.5–1.0s on mid-tier mobile |
| #58 | WE-20260528-1056 | Chat messages had no `lang=` attr — NVDA mispronounced Hindi/Arabic with English phonemes (WCAG 3.1.2 fail) | `isRtlLanguage(code)` helper checking BCP-47 primary subtag; AI markdown wrapper now carries `lang={message.language\|\|'en'}` + `dir` per RTL detection; user-typed `<p>` carries `dir="auto"` for typed bidi text |
| #59 | WE-20260528-103 | 3 inconsistent surfaces for `mode`: client union has `stylist`/`assistant`/etc; server schema enum had `styler` typo + missing `assistant`; `/api/chat/stream` had NO validator → silently swallowed invalid modes | Unified canonical set `planner\|stylist\|knowledge\|assistant`; added `assistant` to schema; mounted `validateBody(ChatRequestSchema)` on `POST /api/chat/stream`; 12-case regression test pins accept/reject set |
| #60 | WE-20260528-1051 | 18 auth inputs (email/password/OTP/phone) had no `autocomplete=` → password managers couldn't autofill (WCAG 2.2 SC 3.3.8 fail) | 18 attribute additions across SignInModal / SignUpModal / PhoneInput per WCAG 2.2 mapping; Playwright live-DOM proves each input carries the correct value |
| #61 | WE-20260528-1063 | Many icon-only buttons across ChatHeader / ChatInput / ImageActions / ImageCarousel / AudioPlayer had no `aria-label` → Voice Control + Switch Control users couldn't target them (WCAG 2.5.3 + 4.1.2 fails) | 24 verb+object aria-labels (including dynamic mic states); ChatMessages left for sibling -1056 agent |
| #65 | WE-20260528-1055 | `useTypewriter` JS hook used RAF at 12ms/char → bypassed CSS `prefers-reduced-motion` reset → users with reduce-motion saw 5–30s char-by-char animation (WCAG 2.2.2 fail) | `matchMedia('(prefers-reduced-motion: reduce)')` subscription (Safari <14 fallback + SSR-safe initial read); both effects gated: matched=instant `displayedText`, unmatched=byte-identical previous. Behavioral hook test 4/4 pass |
| #66 | WE-20260528-305 | PostHog `initAnalytics` ran synchronously before `createRoot` → posthog-js 50KB gz inline in main bundle + `/ingest/*` requests competed with main JS download | `<AnalyticsBoot/>` in App.tsx fires on mount via `requestIdleCallback(2s timeout)` to dynamic-import analytics; `lib/analytics` now `import type` + Proxy + 200-entry FIFO queues per export to buffer pre-init calls without losing events; `vendor-posthog` removed from modulepreload chain (−59KB gz initial / −7.6%) |

### P2 + P3 (visual + UX polish)

| PR | Ticket | What was broken | How it was fixed |
|---:|---|---|---|
| #62 | WE-20260528-861 | TimelineView empty-state guard missed `entries=0 && weddingDate` case → user with wedding date but no events saw broken-looking axis | Guard flipped to `entries.length===0` alone; body branches on `weddingDate` — has-date shows Heart icon + 'Your wedding is on {date}' + 'Add an event' + 'Go to checklists' CTAs |
| #63 | WE-20260528-884 | NotificationPanel empty copy promised reminders to free-tier user with no checklists → dead-end UX | 4-branch state machine: no-checklists → Planner CTA / no-due-dates → Add dates / paid-no-collab → Invite Partner / fallback; new optional `isPremium` + `onNavigate` props (backward-compat) |
| #64 | WE-20260528-896 | ChecklistDetail and ShoppingListView had inconsistent empty states (one had pointer, other was dead text) | Harmonized both on ChecklistDetail's superior CTA pattern: `+ Add the first item` link that opens the add-item form |
| #67 | WE-20260528-897 | BudgetDashboard footer rendered literal "0% used" / "0 left" when categories=[] → "0% of 0" confusion | Placeholder dashes "—% used" / "— left" in muted text when no categories; preserves footer layout rhythm |
| #68 | WE-20260528-860 | Sonner toaster hardcoded `theme="dark"` ignoring `useTheme()` hook → light-mode users saw dark toasts | 1-line fix: `theme={theme as ToasterProps["theme"]}`; hook return now actually consumed |
| #69 | WE-20260528-892 | AttachmentPicker empty states were dead-end text ("No notes yet, Create one in Notes") with no action → 6-click user dance | Extended EmptyState with optional `actionLabel`/`onAction`; `goTo()` helper closes picker on next animation frame then navigates; 4 tab CTAs (Open Notes/Planner/Timeline/Gallery) gated on userId |
| #70 | WE-20260528-864 | SharedChat/SharedNote had no print stylesheet → PDFs included gradient bg + floater + CTA banner + consent banner + bubbles split mid-sentence | Hybrid: marker class `.shared-print-surface` + inline Tailwind `print:*` utilities + central `@media print` block in index.css for pseudo-elements & globally-mounted fixed banners (via `body:has()` selectors); `page-break-inside:avoid` on bubbles |
| #71 | WE-20260528-893 | NoteShareDialog showed blank list area when note had no shares → users wondered if dialog was broken | Inline empty-state: Share2 icon + 'No shares yet' + sub-copy + ArrowUp cue for owners; non-owners see alt copy; existing controls untouched |
| #72 | WE-20260528-880 | Settings tabs on mobile remounted on Back/re-enter → scroll position lost (paper cut on Plan & Billing) | `mobileScrollRef` + `scrollPositionsRef` Map<SettingsTabId, number>; snapshot on Back tap + restore via `useLayoutEffect` + `requestAnimationFrame`; mobile-only |

### In flight at snapshot (3, will land within ~15 min)

| Ticket | What's being fixed |
|---|---|
| WE-20260528-870 | Note + checklist titles need 80-char cap + editor-header truncation (P3 visual) |
| WE-20260528-1066 | `animate-pulse` + `shimmer` + `animate-in` need `motion-safe:` prefix to honor reduce-motion (P3 a11y) |
| WE-20260528-107 | XSS in shared-chat threadTitle — sanitize on write + render as plain text on SharedChat (P2 security) |

---

## Backlog — needs Krish

Cannot be fix-agent'd autonomously. See `qa-harness/sheet-export/marathon-backlog-2026-05-29.csv` for the full row data. Summary:

### Locked decisions / policy
1. **Pricing $10/$79 vs locked $14.99/$119** (WE-20260528-011/012/804) — third sprint unfixed; revenue leak today. Confirm price OR update locked-decision memory.
2. **GDPR Right-to-Erasure** (WE-20260528-1001) — `/api/account/delete` is no-op flag. Strategy call: build purge Cloud Function (Krish runs Firebase deploy) / shorten retention / risk-accept.
3. **S2 -760 + -767** — pricing prompt anchor + distress-handling rail wording; legal/SME review.

### Security triad (needs auth-model decision)
4. **WE-20260528-202** — `requireAuth` silently permissive (guest pass-through everywhere)
5. **WE-20260528-248** — CORS `*` + guest pass-through (drive-by from any origin)
6. **WE-20260528-001** — `/api/speech-token` returns Azure JWT without auth
7. **WE-20260528-1002–1010** — provider enumeration, shared-note PII, X-Forwarded-For spoof, feedback impersonation, requireStrictAuth fail-open, CORS mutation preflight, per-process rate limiter (no shared store), raw `err.message` leak across endpoints

Tightening any of these breaks the guest-flow contract. **Needs Krish go-ahead before fix-backend touches.**

### Architectural decisions
8. **BC-ARCH** — BroadcastChannel adoption strategy. Closes ~25 cross-tab desync tickets if landed: WE-20260528-552/-967 (logout), -987 (deleted thread), -991/-999 (tier upgrade), -993 (provider linking), -996 (notification badge), etc.
9. **PERF-BATCHING** — Sprint 1 perf branches -301..-315 + S3 -1101..-1122 not yet shipped. Strategy: separate `perf/sprint-bundle` branch vs individual PRs.
10. **WE-20260528-851** — RTL infrastructure missing (2-day refactor; closes 7 i18n/RTL tickets); blocker for UAE/Saudi GTM per AED pricing.

### Product / scope decisions
11. **WE-20260528-1078** — PaymentFailure page generic copy. Needs PayU error-code → plain-English mapping table.
12. **WE-20260527-001, -016, -204** — three placeholder branches from yesterday with no commits; scope unclear when first attempted.

### Tooling
13. **ESLINT-FUNCTIONS** — `eslint 9.39.4` vs `@typescript-eslint/no-unused-expressions` plugin mismatch in `functions/` subdir crashes repo-wide `npm run lint`. Pre-existing; affects every fix-frontend PR's lint step. Bump plugin / scope ESLint to `src/` / pin eslint <9.

---

## Coverage map vs the 545-ticket queue

| Category | Total | Covered today | Remaining |
|---|---:|---:|---:|
| Security | 50 | 5 (PRs #50, #57, #70, in-flight #107) + 4 from yesterday | mostly Krish-blocked security triad |
| State-sync | 80 | 4 (PRs #51, #54, #55) + 2 from yesterday | mostly waiting on PR #32 merge to close AuthContext children; BC-ARCH cluster blocked |
| Perf | 29 | 3 (PRs #52, #56, #66) + 1 from yesterday | -301/-1101/-1102/-1103/-1114/-1117 batched in PERF-BATCHING decision |
| A11y | 50+ | 4 (PRs #58, #60, #61, #65) + in-flight #1066 + 3 from yesterday | -1067 stream-complete SR, others |
| Functional / contract | 30+ | 3 (PRs #53, #59) + 2 from yesterday | -805 guest signup data loss, -110 SSE Last-Event-ID |
| Visual / responsive | 80+ | 7 (PRs #62, #63, #64, #67, #68, #69, #70, #71, #72) | bulk P3 polish + RTL/i18n blocked on Krish |

---

## Files for Krish

- **Master tracker CSV (this marathon)**: `qa-harness/sheet-export/marathon-master-2026-05-29.csv` → pushed to Google Sheet (URL below)
- **Backlog CSV (Krish-decision items)**: `qa-harness/sheet-export/marathon-backlog-2026-05-29.csv` → pushed to Google Sheet (URL below)
- **This report**: `qa-harness/MARATHON-2026-05-29.md`
- **Per-ticket evidence**: `qa-harness/evidence/WE-20260528-*/`
- **Per-fix progress logs**: `qa-harness/progress/WE-20260528-*/progress.html`
- **All worktrees**: `worktrees/fix-WE-*/` (preserved for chairman review)

## Google Sheet URLs

(filled below after Drive MCP push)

---

## How to review

1. Skim the headline table above.
2. For each PR, the GitHub URL has: full diff + PR body with "What was broken" + "What this PR does" + test results.
3. The per-PR `progress.html` audit trail walks through reproduce → diagnose → fix → test.
4. Approve via GitHub → merges to `Bug-Resolve-claude`.
5. When `Bug-Resolve-claude` is approved by you, merge to main (manual).

## Recommended merge order (to minimize conflicts)

- Merge state-sync / AuthContext first: **#32** (root) → then #51, #54, #55, #58, #63, #71
- Merge perf bundle in graph order: **#49 (NotesView lazy)** → **#52 (chunk graph)** → **#56 (Index lazy)** → **#66 (PostHog defer)**
- Merge security: #50, #57, #70 (no conflicts)
- Merge contract: #59 (no conflicts)
- Merge a11y: #45, #58, #60, #61, #65 (light conflicts on ChatMessages/ChatInput possible)
- Merge UX polish: #62, #63, #64, #67, #68, #69, #71, #72
- Yesterday's queue #32–#43 land independently per their original cadence

---

**Marathon status: 23 PRs in, 3 in flight, queue cleanly partitioned between landable + Krish-decision. Master + backlog sheets ready for review.**
