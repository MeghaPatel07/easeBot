---
id: WE-20260528-973
title: Listener leak — opening 3 different threads in sequence each spawns a `subscribeToMessages` that is never called (function exported, never used in Index)
category: state-sync
severity: P2
date: 2026-05-28
component: chatService.subscribeToMessages, Index.tsx loadChat
status: open
sprint: 3
related: WE-20260528-558
---

## Steps to reproduce
1. Code review: `grep -rn "subscribeToMessages" Wedding-Ease-Viva-Chat/src` returns only the export site + ChatMessages.tsx hovered import. Index.tsx uses one-shot `loadLatestMessages` (useChat.ts:952), not the live listener.
2. Open thread A. Switch to thread B. Switch back to A. New messages from another tab will NOT appear in either thread.

## Expected
Either remove the unused `subscribeToMessages` export, or use it to enable live message sync.

## Actual
- Dead-code export. Live message sync feature missing.

## Severity rationale
P2 — Sprint 2 -558 already covers cross-tab. This is the lower-priority code-hygiene angle.
