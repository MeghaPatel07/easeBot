---
id: WE-20260528-572
title: Optimistic UI for theme toggle has no rollback path when offline; AccountTab.tsx:172 comment claims "rollback snapshot now includes phone fields" but theme has no equivalent rollback
category: state-sync
severity: P2
date: 2026-05-28
component: ThemeContext / AppearanceTab
status: open
sprint: 2
related: WE-20260528-555
---

## Steps to reproduce
1. Toggle DevTools → Network → Offline.
2. Toggle theme in Appearance tab.
3. Toast: none. Visually: theme flips. Backend write: fails silently.
4. Bring network back. Reload.
5. Theme reverts to old value (because backend never accepted the write).

## Expected
Either show "Couldn't save your preference" toast OR queue the write and retry on reconnect.

## Actual
Same root cause as -555 but observed via the AppearanceTab path specifically (uses `useAccount.updatePreferences` which DOES surface errors per `useAccount.ts:44` comment — but AppearanceTab may not be propagating to a toast). Need to verify AppearanceTab catches and shows.

## Status
Needs read of `src/pages/settings/tabs/AppearanceTab.tsx` to confirm whether the mutation result is surfaced. Logging as separate ticket from -555 because -555 is about ThemeContext fire-and-forget; this is about AppearanceTab error UX.

## Evidence
- `src/pages/settings/tabs/AppearanceTab.tsx:5` (comment claims optimistic with rollback)
- Need live test to confirm toast appears
