# WE-20260528-1221: `vibe_submitted` PostHog event ships `vibe` slug only — no guarantee the slug is sanitized; user-authored custom vibe titles flow through `custom_vibe_created` without char-cap on event prop

| Field | Value |
|---|---|
| **ID** | `WE-20260528-1221` |
| **Created** | `2026-05-28T17:47Z` |
| **Reporter** | `qa-functional` |
| **Severity** | `P3` |
| **Category** | `edge` (analytics-data integrity) |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/hooks/useImageHubSubmit.ts:85`, `src/components/images/CustomVibeForm.tsx:63` |
| **URL / Page** | Images Hub → submit/custom |
| **Breakpoint** | `all` |
| **Status** | `new` |

## Description

Two related PostHog events:

- `track('vibe_submitted', { vibe: vibeSlug })` at useImageHubSubmit.ts:85 — vibeSlug origin is unclear; if it's the FE selection it's a known set. If it's the result of `forceImageGeneration` + user-typed text, it can be arbitrary.
- `track('custom_vibe_created', {})` at CustomVibeForm.tsx:63 — currently ships empty props. SHOULD ship the vibe descriptors so the team can measure adoption, BUT user-typed descriptors are PII-adjacent (style memory: "my partner Maya's purple gown") — a length cap + scrubber is needed before adding.

The gap: the form accepts user-typed `vibeTitle` (1-60 chars per `schemas/chat.ts:18` BE-side) but the PostHog event side has neither cap nor scrubber if title is ever added.

## Notes

Informational for now since `custom_vibe_created` ships `{}`. File for the next time someone enriches the payload — there's no allowlist (see -1204).
