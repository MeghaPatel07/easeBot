# WE-20260528-1217: Summarizer + `max_tokens: 300` exceeds the "~200 token summary" docstring promise — call should be 200, not 300

| Field | Value |
|---|---|
| **ID** | `WE-20260528-1217` |
| **Created** | `2026-05-28T17:45Z` |
| **Reporter** | `qa-functional` |
| **Severity** | `P3` |
| **Category** | `contract` (spec) |
| **Repo** | `easebot-backend` |
| **Path** | `src/services/conversationSummarizer.ts:11,45` |
| **URL / Page** | `POST /api/chat` summarizer call |
| **Breakpoint** | `all` |
| **Status** | `new` |

## Description

Docstring at `conversationSummarizer.ts:11`:
> Produces a ~200 token summary of the provided messages using a cheap/fast LLM call.

Actual call at line 45 sets `max_tokens: 300`. The summary can be 50% larger than documented. Each summarizer call costs 33% more than the spec implies.

Also the model is `process.env.AZURE_DEPLOYMENT_NAME!` — same model as the main chat completion (not a "cheap/fast" sibling). Spec violation: should be a smaller/cheaper deployment.

## Steps to reproduce

1. `cd easebot-backend && grep -n max_tokens src/services/conversationSummarizer.ts`
2. Compare to docstring at line 11.

## Expected

Either docstring updated to "~300 token" + ROI re-estimated, or `max_tokens: 200` + use a cheaper deployment (e.g. gpt-4o-mini equivalent).

## Actual

300 max + main model.

## Notes

Fix-backend-api. Tiny but compounds cost across high-volume threads. Pair with -1216 (observability) so we can measure summarizer spend per cohort.
