---
id: WE-20260528-971
title: BroadcastChannel API unused — every cross-tab sync gap could be solved by a single BC instance + handler map; foundation missing
category: state-sync
severity: P1
date: 2026-05-28
component: app-wide
status: open
sprint: 3
related: WE-20260528-553
---

## Steps to reproduce
1. `grep -rn "BroadcastChannel\|new BroadcastChannel" Wedding-Ease-Viva-Chat/src` returns 0.
2. Review Sprint 2 -551 to -575: at least 12 of the 25 cross-tab bugs would be one-liners with a BC pub/sub.

## Expected
A shared `bc.ts` module exposing `publish(topic, payload)` + `subscribe(topic, handler)` used by ThemeContext, AuthContext, ConsentBanner, theme-toggle, sign-out, settings-save, mode-switch.

## Actual
- Not present. Each cross-tab bug needs its own bespoke fix.

## Suggested owner
fix-state-data
