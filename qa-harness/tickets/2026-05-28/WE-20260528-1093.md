# WE-20260528-1093: Reduced-motion + dark mode combo not regression-tested — typewriter is the canary

| Field | Value |
|---|---|
| **ID** | `WE-20260528-1093` |
| **Created** | `2026-05-28T17:24:00Z` |
| **Reporter** | `a11y-qa-sprint3` |
| **Severity** | `P3` |
| **Priority** | `low` |
| **Category** | `a11y` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/hooks/useTypewriter.ts` + theming files |
| **URL / Page** | `/` |
| **Breakpoint** | `all` |
| **Status** | `new` |
| **WCAG** | meta — process gap |

## Description

QA process gap: the visual regression sweep (qa-screenshots.mjs) captures light/dark mode and several viewports, but does NOT toggle `prefers-reduced-motion`. So bugs like -1055 (typewriter ignoring reduced-motion) escape detection. Reduced-motion + dark + mobile is a real combination for, e.g., a VoiceOver iPhone user in a dim room.

## Expected

Add `prefers-reduced-motion` toggle to QA harness. Capture screenshots with motion-reduce ON for each (theme × breakpoint). Compare structural diff vs motion-on baseline — flag visual divergence beyond expected.

## Actual

Reduced-motion not in regression matrix.

## Notes

Process-level. Doesn't block a release but blocks the next sprint's confidence.
