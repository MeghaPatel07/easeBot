# WE-20260528-1119 — Sprint 1 -315 (SSE first-token blocker) STILL open — perf-team cannot measure streaming throughput

**Severity:** P2 (carried-over blocker, not a new finding)
**Status:** triaged
**Assigned:** fix-performance
**Area:** Performance / SSE measurement
**Cross-ref:** WE-20260527-315
**Discovered:** 2026-05-28 (carried)

## Note
Sprint 1 already filed `WE-20260527-315 — Could not measure SSE first-token latency / image-gen latency — backend health endpoint returns 404`. We confirmed today that `/api/health` now works (200 OK, uptime 15957s, content `{"status":"ok"}`), so the original blocker symptom is gone — but the *underlying* measurement gap remains.

What we still cannot measure:
- Time-to-first-chunk on /api/chat/stream (needs auth — see -1108)
- Tokens/sec sustained for a 1000-token Azure OpenAI response
- End-to-end with tool execution (create_checklist Firestore write + second LLM pass)

## What we CAN say
- Unauth SSE round-trip with body-validation error: 43ms total — the SSE wrapper itself is not the bottleneck.
- The chat SSE route does add heartbeat (15s interval, 17 char comment per beat) and per-frame event-id injection (~30 bytes/frame) — negligible bandwidth overhead.

## Action
Pair with -1108 (auth bypass for perf QA). Once unblocked, retire this ticket with concrete tokens/sec numbers.

## Evidence
- Original: `/Users/krish/Desktop/easebot/qa-harness/tickets/2026-05-27/WE-20260527-315.md`
- New: `/Users/krish/Desktop/easebot/qa-harness/evidence/perf-sprint3/health-seq.log`
