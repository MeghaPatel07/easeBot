# WE-20260528-896: ChecklistDetail "No items yet" empty state — copy is text-only with no "Add item" button, but the picker form is always visible above the list; copy redundant + button missing

| Field | Value |
|---|---|
| **ID** | `WE-20260528-896` |
| **Created** | `2026-05-28T23:40:00Z` |
| **Reporter** | `qa-visual-sprint3` |
| **Severity** | `P3` |
| **Priority** | `P3` |
| **Category** | `visual` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/components/ChecklistDetail.tsx:316` (`<p>No items yet.</p>`), `src/components/ShoppingListView.tsx:551` |
| **URL / Page** | /planner → open empty checklist; /shopping empty |
| **Breakpoint** | all |
| **Status** | `new` |
| **Assigned** | fix-frontend |

## Description

Two surfaces show `<p>No items yet</p>` (or close to it):
- ChecklistDetail.tsx:316.
- ShoppingListView.tsx:551 — "No items yet. Add your first item above."

The shopping-list copy includes "Add your first item above" pointing to the existing input. ChecklistDetail's version doesn't. Inconsistency.

Better: both should either have an inline CTA arrow pointing UP to the existing input, OR have a primary "Add first item" button that focuses the input.

## Steps to reproduce

1. Open /planner → create empty checklist → open it.
2. Observe "No items yet" with no guidance.

## Expected

Consistent empty-state pattern that points to the existing add-input or has a focus-the-input button.

## Actual

Inconsistent copy across the two surfaces.

## Evidence

- ChecklistDetail.tsx:316 and ShoppingListView.tsx:551 cited.

## Notes

Sub-case of -855.

---
_Filed by `qa-visual-sprint3` on `2026-05-28T23:40:00Z`._
