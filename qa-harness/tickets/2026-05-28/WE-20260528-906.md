# WE-20260528-906: No /api/calendar route exists — Sprint 3 reminder/event API entirely missing from backend

| Field | Value |
|---|---|
| **ID** | `WE-20260528-906` |
| **Created** | `2026-05-28T09:30:00Z` |
| **Reporter** | `qa-e2e-playwright` |
| **Severity** | `P2` |
| **Priority** | `P2` |
| **Category** | `contract` |
| **Repo** | `easebot-backend` |
| **Path** | `src/app.ts:88-103` (no calendar mount) |
| **URL / Page** | `POST /api/calendar` |
| **Breakpoint** | n/a (API) |
| **Status** | `new` |
| **Assigned** | fix-backend |

## Description

Sprint 3 task #4 was "Reminder save — POST /api/calendar with valid event, recurring event, conflicting event, past-date event". Spider of the backend shows:

- No `routes/calendar.ts` file.
- No `mountRoutes` entry in `src/app.ts:88-100`.
- `grep -rE "calendar|reminder" src/routes/ src/controllers/` returns no matches.

`POST /api/calendar` returns the Express default-404 HTML body.

This is either a not-yet-implemented endpoint (scope creep on the sprint plan) or a removed endpoint nobody updated the spec for. Either way it's a contract gap — the frontend is presumably either using Firestore directly for reminders or there is no reminder feature wired through the API at all.

## Steps to reproduce

```bash
curl -isS -X POST http://localhost:3001/api/calendar \
  -H 'Content-Type: application/json' \
  -d '{"title":"Test reminder","date":"2026-06-01T10:00:00Z"}'
# → 404, default HTML
ls easebot-backend/src/routes/ | grep -i calendar
# → (no output)
```

## Expected

If the feature is in scope: a `routes/calendar.ts` with at minimum POST/GET/DELETE behind `requireAuth`, edge-cases for past dates / conflicting time-slots / recurrences (RRULE).

If not in scope: remove the assumption from the sprint spec.

## Actual

Endpoint absent. Sprint task is non-startable as written.

## Evidence

- `easebot-backend/src/app.ts:88-100` — full route mount table, no calendar
- `easebot-backend/src/routes/` directory listing — no calendar file

## Notes

- This may overlap with the Notes-GTM staircase plan (D1 unstarted per project memory). Confirm scope with Krish before building.

---

_Filed by `qa-e2e-playwright` on `2026-05-28T09:30:00Z`. Synced to sheet row when `/qa-bug-report` runs._
