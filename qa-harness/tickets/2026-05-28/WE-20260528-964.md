---
id: WE-20260528-964
title: visibilitychange / document.hidden — ZERO listeners; cannot pause useUsageStats refetchInterval when tab is hidden (wastes battery + tokens)
category: state-sync
severity: P2
date: 2026-05-28
component: useUsageStats refetchInterval 60000
status: open
sprint: 3
---

## Steps to reproduce
1. `grep -rn "visibilitychange\|document.hidden\|document.visibilityState" Wedding-Ease-Viva-Chat/src` returns 0 hits.
2. Open app, leave tab in background overnight.
3. Inspect network: useUsageStats fires `/api/account/usage` every 60s, 480 requests in 8h while tab is invisible.

## Expected
Pause polling when `document.hidden === true`; refetch once on visibility flip.

## Actual
- Always polls. TanStack Query has `refetchIntervalInBackground` default false, but for `useUsageStats` the inner setInterval is set unconditionally with no visibility check.
- Bonus: battery / data wasted; PostHog session-replay continues to record an invisible tab.

## Suggested owner
fix-perf
