# WE-20260528-1216: `conversation_summarized` PostHog event declared but never fired — funnel/cohort blind to summarizer behavior

| Field | Value |
|---|---|
| **ID** | `WE-20260528-1216` |
| **Created** | `2026-05-28T17:44Z` |
| **Reporter** | `qa-functional` |
| **Severity** | `P3` |
| **Category** | `edge` (observability) |
| **Repo** | `easebot-backend` |
| **Path** | `src/lib/posthog.ts:49`, `src/controllers/chatController.ts:717-733,1222-1238` |
| **URL / Page** | `POST /api/chat` server-side |
| **Breakpoint** | `all` |
| **Status** | `new` |

## Description

The backend PostHog event type `'conversation_summarized'` exists in the event union at `lib/posthog.ts:49`. The summarizer fires on every >10-message request (see -1207). It is **never captured** to PostHog.

Funnel analysis ("does summarizer trigger correlate with retention drop / token-spike / cap-hit?") is impossible.

Same gap for `chat.summarization_failed` observability emit (see -1208) — referenced as a type but no call site.

## Steps to reproduce

1. `cd easebot-backend && grep -rn "'conversation_summarized'" src/ --include="*.ts"` → returns only the type union at posthog.ts:49.
2. Send 11+ messages. PostHog Live events → no `conversation_summarized` ever appears.

## Expected

`phCapture(uid, 'conversation_summarized', { msg_count_in: olderMessages.length, summary_chars: summary.length, mode })` immediately after the summary returns successfully.

## Actual

Type exists, no call site.

## Notes

Fix-backend-api. Trivial — same call pattern as `payu_initiated` at paymentController.ts:203.
