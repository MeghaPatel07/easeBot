# WE-20260528-002: SSML injection in `/api/tts` via unsanitized `voiceName` allows arbitrary SSML element substitution

| Field | Value |
|---|---|
| **ID** | `WE-20260528-002` |
| **Created** | `2026-05-28T05:52Z` |
| **Reporter** | `qa-functional-megha` |
| **Severity** | `P0` (security) |
| **Priority** | P0 |
| **Category** | `functional` / security |
| **Repo** | `easebot-backend` |
| **Path** | `easebot-backend/src/services/azureTTS.ts:101-105`, `easebot-backend/src/controllers/ttsController.ts:27` |
| **URL / Page** | `http://localhost:3001/api/tts` |
| **Breakpoint** | all |
| **Status** | `new` |
| **Assigned** | fix-backend-api |

## Description

The TTS controller accepts `voiceName` from `req.body` with **zero validation** (no Zod schema; the request body is read straight off `req.body`). `resolveAzureVoice` returns the user-supplied string verbatim if it matches `/Neural$/`. That string is then interpolated unescaped into the SSML `<voice name="${azureVoice}">` template (`azureTTS.ts:102`). An attacker can break out of the attribute and inject arbitrary SSML elements.

This bypasses the existing `escapeSSML` (which is only applied to `text`, not `voiceName`).

## Steps to reproduce

```
# Tag injection — closes the <voice> element and opens a new one with a different voice
curl -sS -X POST http://localhost:3001/api/tts \
  -H 'Content-Type: application/json' \
  -d '{"text":"hello","voiceName":"x\"></voice><voice name=\"en-US-JennyNeural"}' \
  --max-time 5 -i
```

→ HTTP 200, body is a 38 KB WAV (Azure synthesized using the **attacker-chosen** voice, not the one the app intended).

```
# Adversarial prosody/style injection
curl -sS -X POST http://localhost:3001/api/tts \
  -H 'Content-Type: application/json' \
  -d '{"text":"hi","voiceName":"en-US-AriaNeural\"/><prosody rate=\"slow\"/><voice name=\"en-US-JennyNeural"}'
```

→ HTTP 500, error message leaks Azure SDK internals: `"Azure TTS cancelled: Node [speak] with type [RootSpeak] should not contain node [prosody] with type [Others]. websocket error code: 1007"` (vendor-error-leak similar to WE-20260527-353).

## Expected

1. `ttsController` validates `voiceName` against a whitelist (e.g. Zod enum of allowed Azure voice IDs).
2. SSML interpolation either uses a strict whitelist or escapes quotes/angle-brackets.
3. Vendor error messages are not surfaced to the client.

## Actual

- `voiceName` is `req.body.voiceName as string` with no parse.
- SSML template uses raw string interpolation.
- Azure SDK error string is echoed back via `res.status(500).json({ error: err.message })`.

## Evidence

- `qa-harness/evidence/WE-20260528-002/repro.sh` — both PoC curls + responses.

## Notes

Related: WE-20260527-210 (TTS vocalizes literal HTML), WE-20260527-216 (schema/controller field-name drift). Together these show the entire `/api/tts` surface has no Zod validation.

---

_Filed by `qa-functional-megha` on `2026-05-28T05:52Z`._
