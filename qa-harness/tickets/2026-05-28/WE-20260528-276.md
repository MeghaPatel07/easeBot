# WE-20260528-276: SharedChat page (/share/:shareId) — no h1, no main landmark

| Field | Value |
|---|---|
| **ID** | `WE-20260528-276` |
| **Created** | `2026-05-28T12:06:00Z` |
| **Reporter** | `a11y-qa` |
| **Severity** | `P1` |
| **Priority** | `high` |
| **Category** | `a11y` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/pages/SharedChat.tsx` |
| **URL / Page** | `/share/:shareId` |
| **Breakpoint** | `all` |
| **Status** | `new` |
| **WCAG** | 1.3.1 Info and Relationships (Level A); 2.4.6 Headings and Labels (AA) |

## Description

Public-facing share view (likely the first impression for non-customers) has h1 count = 0 and zero landmarks. Title is the generic app title, not the conversation subject. Screen-reader users cannot determine what they are viewing.

## Expected

`<h1>` with thread title or "Shared chat: {title}"; `<main>` wrapping the rendered conversation.

## Evidence

`qa-harness/evidence/a11y-2026-05-28/_extra.json` → `share.h1: 0`, `landmarks: 0/0/0/0`.

## Notes

Tested with `/share/test-share-id` which likely 404s server-side but renders a client-side fallback. Re-verify with a valid share ID.

---

_Filed by `a11y-qa` on `2026-05-28T12:06:00Z`._
