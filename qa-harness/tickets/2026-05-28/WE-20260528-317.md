# WE-20260528-317: 68 separate lucide-react import sites — verify tree-shaking is fully effective

| Field | Value |
|---|---|
| **ID** | `WE-20260528-317` |
| **Created** | `2026-05-28T11:30:00Z` |
| **Reporter** | `qa-performance` |
| **Severity** | `P3` |
| **Priority** | `P3` |
| **Category** | `perf` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/components/**/*.tsx` |
| **URL / Page** | `all routes` |
| **Breakpoint** | `all` |
| **Status** | `new` |
| **Assigned** | fix-performance |

## Description

68 files import from 'lucide-react'. lucide-react v0.x should tree-shake correctly when imports are named, but production builds should be audited. The vendor chunk size (324KB gzipped) suggests possible inclusion bloat.

Spot-check: ChatMessages.tsx imports 24+ icons in a single statement at lines 4-11. With ESM tree-shaking these should be fine; with rollup default settings + the absence of explicit manualChunks (312), they may bundle predictably but in the wrong chunk.

## Steps to reproduce

1. grep -rn "from 'lucide-react'" Wedding-Ease-Viva-Chat/src | wc -l → 68
2. Run rollup-plugin-visualizer or vite-bundle-visualizer to confirm lucide footprint in vendor chunk

## Expected

lucide-react contributes ≤ 20KB gzipped to vendor chunk after tree-shake.

## Actual

68 import sites, no measurement of resulting footprint.

## Evidence

- Lighthouse JSON: `qa-harness/evidence/perf-2026-05-28/lh-*-prod.json`
- Build log: `/tmp/perf-build.log`
- Screenshot: `qa-harness/evidence/perf-2026-05-28/*.jpg`

## Notes

Low priority — verify only. If tree-shake is fine this is no-op.

---

_Filed by `qa-performance` on `2026-05-28T11:30:00Z`._
