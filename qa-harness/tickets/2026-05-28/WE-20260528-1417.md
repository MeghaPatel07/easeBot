# WE-20260528-1417: Sprint-4 reconfirmation — `/api/chat` returns 200 with no `Authorization` header. requireAuth still permissive (see -202)

| Field | Value |
|---|---|
| **ID** | `WE-20260528-1417` |
| **Created** | `2026-05-28T20:05:00Z` |
| **Reporter** | `edge-case-qa` (sprint-4 backend-chaos) |
| **Severity** | `P0` |
| **Priority** | `P0` |
| **Category** | `edge` |
| **Repo** | `easebot-backend` |
| **Path** | `src/middleware/auth.ts` |
| **URL / Page** | `POST /api/chat` |
| **Breakpoint** | `all` |
| **Status** | `dup_of:WE-20260528-202` |

## Description

```bash
$ curl -s -X POST http://localhost:3001/api/chat -H "Content-Type: application/json" -d '{"message":"hi"}'
{"text":"Hi there! How's the wedding planning going?", ... }
```

No `Authorization` header. 200 OK with full LLM reply. Confirms -202 still open. Every Sprint-4 cost-leak ticket cited above amplifies because of this: anonymous attackers can exercise all of -1401 (cost leak), -1402 (validation bypass), -1405 (image rate-limit bypass) without auth.

## Expected

Per -202 root cause: `requireAuth` must reject when no `Authorization` header is present (or treat as guest with hard caps).

## Tags

`sprint-4`, `chaos`, `regression-confirm`, `auth`, `dup_of:-202`
