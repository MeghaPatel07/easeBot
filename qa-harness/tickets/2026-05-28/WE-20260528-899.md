# WE-20260528-899: ChatHeader/ProfileMenu external links commented out per recent commit 8944b30 — surfaces broken-feeling app: tap user avatar → menu has no useful items beyond Sign Out

| Field | Value |
|---|---|
| **ID** | `WE-20260528-899` |
| **Created** | `2026-05-28T23:46:00Z` |
| **Reporter** | `qa-visual-sprint3` |
| **Severity** | `P2` |
| **Priority** | `P2` |
| **Category** | `visual` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/components/chat/ChatHeader.tsx`, `src/components/chat/ProfileMenu.tsx` (commented-out links per commit 8944b30) |
| **URL / Page** | /chat → click profile avatar |
| **Breakpoint** | all |
| **Status** | `new` |
| **Assigned** | fix-frontend |

## Description

Per git log (commit 8944b30: `refactor: comment out external links in ProfileMenu and ChatHeader for future adjustments`), several profile-menu items are commented out. The remaining menu contains only Settings + Sign Out — historically there were "Account", "Pricing", "Help" etc. Users who recall those entry points hit a depopulated menu.

The intent (per commit) was temporary while a new layout lands. Filing this so the WIP state doesn't ship to prod without restoring some entry points. Verify which links are intentionally hidden vs accidentally hidden.

## Steps to reproduce

1. Open /chat → click avatar/profile in sidebar.
2. Observe menu — sparse compared to recent versions.

## Expected

Either restore commented-out links OR explicit decision documented (settings.local should not include WIP UI states by default).

## Actual

Depopulated menu, looks unfinished.

## Evidence

- git commit 8944b30.
- ChatHeader.tsx + ProfileMenu.tsx contain `// <removed>` blocks (audit).

## Notes

This is sprint-state hygiene; not a layout bug per se but it surfaces visually.

---
_Filed by `qa-visual-sprint3` on `2026-05-28T23:46:00Z`._
