---
id: WE-20260528-963
title: navigator.onLine / online / offline events — ZERO app-wide handlers; no offline banner, no reconnect notification
category: state-sync
severity: P1
date: 2026-05-28
component: app-wide
status: open
sprint: 3
---

## Steps to reproduce
1. `grep -rn "navigator.onLine\|addEventListener.*'online'\|addEventListener.*'offline'" Wedding-Ease-Viva-Chat/src` returns 0 hits.
2. With app open, DevTools, Network, Offline.
3. Observe UI, no banner, no toast, no indication anything is wrong.
4. User attempts to send a message, silent failure (see 951).

## Expected
A small "You're offline — changes will sync when you reconnect" banner appears immediately on `offline` event; clears on `online`.

## Actual
- No detection at all. App appears fully functional until user tries a mutation.

## Suggested owner
fix-state-ui
