---
id: WE-20260528-954
title: No service worker registered — push notifications, offline cache, install-as-PWA all impossible; manifest.json exists but unused
category: state-sync
severity: P2
date: 2026-05-28
component: public/manifest.json, src/main.tsx
status: open
sprint: 3
---

## Steps to reproduce
1. Open DevTools, Application, Service Workers.
2. Observe: "No service workers detected for this origin."
3. `grep -rn "navigator.serviceWorker\|serviceWorker.register\|workbox" Wedding-Ease-Viva-Chat/src` returns 0.
4. `public/manifest.json` exists (linked in index.html) but no SW, so the PWA install prompt never fires.

## Expected
Either remove `manifest.json` to avoid implying PWA support, OR register a service worker for offline static-asset caching + push notification capability (Sprint 2 -575 onboarding reminder use case).

## Actual
- Manifest present, no SW.
- Browser will not offer "Add to Home Screen" / "Install".
- No background notifications possible for reminders (relies on in-app `subscribeToReminders` listener — only fires while tab is open).

## Suggested owner
fix-perf
