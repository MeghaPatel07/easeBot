# WE-20260528-273: Chat ChatMessages still missing role=log / aria-live container — WE-20260527-251 PR #41 not yet merged

| Field | Value |
|---|---|
| **ID** | `WE-20260528-273` |
| **Created** | `2026-05-28T12:02:00Z` |
| **Reporter** | `a11y-qa` |
| **Severity** | `P0` |
| **Priority** | `critical` |
| **Category** | `a11y` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/components/chat/ChatMessages.tsx` |
| **URL / Page** | `/` |
| **Breakpoint** | `all` |
| **Status** | `regression-check` — canonical: `WE-20260527-251` |
| **WCAG** | 4.1.3 Status Messages (Level AA) |

## Description

Re-checking after PR #41 — `grep aria-live` in `src/components/chat/` returns ONE match (ChatInput recording timer, line 322). No `role="log"` anywhere. The streaming assistant reply is still silent to screen readers on `main`. WE-20260527-251 ticket lists status `in_review` with branch `fix-WE-20260527-251` (commit 2352cd4). The fix is on a branch but not merged into `main` (the live state I just scanned).

## Notes

Not a new bug — verification ticket confirming the fix is not yet present on `main`. No action required from a11y-qa beyond reporting.

---

_Filed by `a11y-qa` on `2026-05-28T12:02:00Z`._
