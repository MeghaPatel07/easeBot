# WE-20260528-804: Sprint 3 regression sweep — Pro tier still $10/mo & $79/yr across FE+BE+state-machine (33% revenue undercut PERSISTS, locked = $14.99/$119)

| Field | Value |
|---|---|
| **ID** | `WE-20260528-804` |
| **Created** | `2026-05-28T17:36Z` |
| **Reporter** | `qa-functional-sprint3-regression` |
| **Severity** | `P0` (revenue / pricing-rollout contract) |
| **Priority** | `P0` |
| **Category** | `functional` / `contract` / `regression` |
| **Repo** | `Wedding-Ease-Viva-Chat` + `easebot-backend` |
| **Path** | `easebot-backend/src/controllers/paymentController.ts:42-43`, `easebot-backend/src/services/subscriptionStateMachine.ts` (`pro: { monthly: 10, annual: 79 }`), `Wedding-Ease-Viva-Chat/src/pages/Pricing.tsx:30,53` |
| **URL / Page** | `/pricing`, `POST /api/payment/initiate` |
| **Breakpoint** | all |
| **Status** | `new` |
| **Relates to** | `WE-20260528-011` (FE Sprint 1), `WE-20260528-012` (BE Sprint 1) — both still open |

## Description

Sprint 3 brief regression-checklist line: "WE-20260528-011/012 PRICING REGRESSION — re-confirm Pro tier is still $10 (locked = $14.99). If a CSS / display value disagrees with API value, file separately."

Confirmed unchanged across **three independent surfaces** at 17:36Z:

| Surface | Path | Monthly USD | Annual USD |
|---|---|---:|---:|
| Frontend display | `Pricing.tsx:30` (`PRICING_DATA`) | **10** | **79** |
| Frontend FAQ copy | `Pricing.tsx:53` | **10** | – |
| Backend price catalog | `paymentController.ts:42-43` | **10** | **79** |
| State machine | `subscriptionStateMachine.ts` (`PLAN_USD_TABLE`) | **10** | **79** |
| Locked PRD (memory) | `project_pricing_rollout.md §6` | **14.99** | **119** |

FE and BE values agree with each other (so the new "CSS vs API mismatch" sub-case from the brief does NOT apply — they're consistently wrong, not split-brained). Every PayU initiate today bills the WRONG amount; the hash is computed against `$10`; invoiceService renders `$10`; subscription state machine credits `$10`. This is a uniform undercut, not a display bug.

Delta vs locked:
- Pro monthly: −33% revenue
- Pro annual:  −34% revenue
- Pro Max:     OK ($39 / $299 agree with locked PRD)
- Top-up:      OK ($10 / 2M tokens agree)

This is a 2-sprint-old un-resolved P0 sitting in the codebase. Until ownership clarifies whether the LOCKED PRD value ($14.99) or the SHIPPED value ($10) is the truth, the revenue exposure compounds with every paid signup.

## Steps to reproduce

```bash
grep -n "'pro:" easebot-backend/src/controllers/paymentController.ts
# → usd: 10 (monthly), usd: 79 (annual)

grep -n "monthlyUsd: 10\|monthlyUsd: 14" Wedding-Ease-Viva-Chat/src/pages/Pricing.tsx
# → monthlyUsd: 10

grep -n "monthly:" easebot-backend/src/services/subscriptionStateMachine.ts
# → pro: { monthly: 10, annual: 79 }
```

## Expected

EITHER:
- (a) all three surfaces → `$14.99 / $119` (locked PRD truth), OR
- (b) the locked PRD memory is updated to `$10 / $79` and explicitly logged in `project_pricing_rollout.md` as a deliberate post-Sprint-2 price drop.

Triage must escalate to product owner. This cannot be "fixed in code" without that direction.

## Actual

Code & PRD disagree by ~33% on Pro pricing for the 3rd straight sprint.

## Evidence

- `qa-harness/evidence/WE-20260528-804/repro.log`
- Companion (Sprint 1 originals, still open): `qa-harness/tickets/2026-05-28/WE-20260528-011.md`, `…/WE-20260528-012.md`

## Notes

Per Sprint 3 brief: "If a CSS / display value disagrees with API value, file separately." Verified that FE display matches BE API value — both are $10 — so this stays as ONE ticket. The disagreement is between the code surface and the locked PRD memory.

---

_Filed by `qa-functional-sprint3-regression` on `2026-05-28T17:36Z`._
