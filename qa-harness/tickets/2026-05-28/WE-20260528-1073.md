# WE-20260528-1073: ChatHeader / hero "Hi! I'm here to help..." — h2 used as greeting, no h1 above it on landing page

| Field | Value |
|---|---|
| **ID** | `WE-20260528-1073` |
| **Created** | `2026-05-28T16:44:00Z` |
| **Reporter** | `a11y-qa-sprint3` |
| **Severity** | `P2` |
| **Priority** | `medium` |
| **Category** | `a11y` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/pages/Index.tsx:1544 (`<h2 class="...">Hi!`); no h1 in empty-state |
| **URL / Page** | `/` empty state (guest landing) |
| **Breakpoint** | `all` |
| **Status** | `new` |
| **WCAG** | 1.3.1 Info and Relationships (Level A); 2.4.6 Headings and Labels (AA); 2.4.10 Section Headings (AAA — informative) |

## Description

The greeting in the empty-state is rendered as `<h2>` with NO preceding `<h1>` on the page. Heading-order axe rule fires. SR users navigating by heading (h-key in NVDA, VO+Cmd+H in VoiceOver) get confusing structure — no top-level page anchor.

This may overlap Sprint 1 -257 (heading-hierarchy ticket) but specifically for the index landing — the empty-state h2-without-h1 case.

## Expected

Either:
- Promote to `<h1>` (probably best — this IS the page identity)
- OR add an aria-labelledby on the main landmark referencing a visible-or-sr-only h1

## Actual

h2 with no h1 — SR's "go to top heading" command lands on h2 awkwardly.

## Notes

Possibly already covered by -257; if so, mark dup.
