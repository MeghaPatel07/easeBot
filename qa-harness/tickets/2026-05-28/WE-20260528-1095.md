# WE-20260528-1095: Vibe picker (Images Hub) — gallery preview uses lightbox without focus-trap (extends -282 pattern)

| Field | Value |
|---|---|
| **ID** | `WE-20260528-1095` |
| **Created** | `2026-05-28T17:28:00Z` |
| **Reporter** | `a11y-qa-sprint3` |
| **Severity** | `P1` |
| **Priority** | `high` |
| **Category** | `a11y` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/components/GalleryView.tsx:193`; `src/components/ImageCarousel.tsx:78` |
| **URL / Page** | `/images`, gallery view |
| **Breakpoint** | `all` |
| **Status** | `new` |
| **WCAG** | 2.1.2 No Keyboard Trap (A); 4.1.2 Name, Role, Value (A); 2.4.11 Focus Not Obscured (WCAG 2.2 AA) |

## Description

GalleryView and ImageCarousel both render `fixed inset-0 z-[9999]` overlay divs without `role="dialog"`, no focus trap, no Esc handler verified. Sprint-1 -282 covered ImageActions share modal — these are siblings. Adds 2.4.11 angle: when lightbox opens, the page underneath retains focus order, so Tab moves OUT of the lightbox unexpectedly.

## Expected

Convert all three (ImageActions share, GalleryView preview, ImageCarousel overlay) to Radix Dialog OR a shared Modal primitive.

## Actual

Three separate ad-hoc overlays, all failing the same set of AT requirements.

## Notes

Triage: bundle with -281, -282 as one fix-PR scope.
