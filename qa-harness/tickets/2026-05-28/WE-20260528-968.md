---
id: WE-20260528-968
title: AnalyticsConsent banner — Accept in Tab A; Tab B still shows banner because no `storage` event listener (re-prompts user, GDPR liability)
category: state-sync
severity: P0
date: 2026-05-28
component: AnalyticsConsent component, ph_consent localStorage key
status: in_review
sprint: 3
related: WE-20260528-551, WE-20260528-561
PR: https://github.com/MeghaPatel07/easeBot/pull/51
Progress: Storage-event listener added to AnalyticsConsent. Cross-tab Playwright repro passes (Tab B banner hides in 2ms after Tab A accepts; reverse path -- key removal -- re-shows banner). S2 -551/-561 stay open until BC layer (BC-ARCH, Krish-blocked) lands.
---

## Steps to reproduce
1. First-time visit. Open `/` in Tab A and Tab B. Banner visible in both.
2. In Tab A click "Accept all".
3. Observe Tab B without reload.

## Expected
Banner in Tab B disappears within 1s.

## Actual
- localStorage write in Tab A; Tab B's banner uses `useState` initialized from localStorage at mount. No `storage` event listener — banner persists.
- User re-clicks Accept in Tab B. Two separate `consent_accepted` events to PostHog — analytics record shows the same user "accepted" twice in 5 seconds. GDPR audit confusion.

## Severity rationale
P0 because: (a) duplicate consent records, (b) user friction, (c) consent-record inconsistency raises legal compliance questions.

## Suggested owner
fix-state-data
