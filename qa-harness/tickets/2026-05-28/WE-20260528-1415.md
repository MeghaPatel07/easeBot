# WE-20260528-1415: PayU `handleReturn` redirects to `http://localhost:8080/payment/failure?...` — but dev FE is on `:8081`. Direct confirmation via live curl on `:3001`. Already filed as -004; sprint-4 confirms still broken

| Field | Value |
|---|---|
| **ID** | `WE-20260528-1415` |
| **Created** | `2026-05-28T20:05:00Z` |
| **Reporter** | `edge-case-qa` (sprint-4 backend-chaos) |
| **Severity** | `P0` |
| **Priority** | `P0` |
| **Category** | `edge` |
| **Repo** | `easebot-backend` |
| **Path** | `src/controllers/paymentController.ts:415 (FRONTEND_BASE_URL env)` |
| **URL / Page** | `POST /api/payment/return` |
| **Breakpoint** | `all` |
| **Status** | `dup_of:WE-20260528-004` |

## Description

This sprint-4 regression test confirms WE-20260528-004 is still live. Direct probe:

```bash
$ curl -s -i -X POST http://localhost:3001/api/payment/return \
    -H "Content-Type: application/x-www-form-urlencoded" \
    -d "txnid=A&hash=deadbeef" -w "%{http_code}\n%{redirect_url}\n" -o /dev/null
302
http://localhost:8080/payment/failure?reason=bad_payload
```

Frontend dev server is on `:8081`. `FRONTEND_BASE_URL` env is set to `:8080`. Either rotate the env or change the frontend port.

## Expected

Update `.env` `FRONTEND_BASE_URL=http://localhost:8081` to match current Vite port.

## Tags

`sprint-4`, `chaos`, `payu`, `regression`, `confirms:-004`
