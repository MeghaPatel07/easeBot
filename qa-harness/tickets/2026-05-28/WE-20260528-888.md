# WE-20260528-888: Pricing modal `CheckoutModal` phone-cap maxLength=15 silently truncates international numbers with country code + spaces (e.g. "+91 98765 43210" = 15 chars; "+971 50 123 4567" = 16 chars trims last digit)

| Field | Value |
|---|---|
| **ID** | `WE-20260528-888` |
| **Created** | `2026-05-28T23:24:00Z` |
| **Reporter** | `qa-visual-sprint3` |
| **Severity** | `P1` |
| **Priority** | `P1` |
| **Category** | `visual` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/components/pricing/CheckoutModal.tsx:129`, `src/pages/Checkout.tsx:414` |
| **URL / Page** | /pricing → checkout flow; /checkout |
| **Breakpoint** | all |
| **Status** | `blocked` |
| **Assigned** | fix-frontend |
| **Blocker Reason** | Ticket misfiled — the two `maxLength={15}` instances at the cited lines are on **GSTIN fields**, not phone inputs. There is no phone field in `CheckoutModal.tsx`, `Checkout.tsx`, or `paymentService.ts` (`BillingAddressInput` and `InitiatePaymentRequest` have no phone field). GSTIN is legally exactly 15 chars and is validated by `GSTIN_REGEX` at `Checkout.tsx:29`. Bumping it to 20 would be misleading. The bug-as-described requires (a) adding a phone field to the checkout form and (b) extending the PayU initiate payload — much larger scope than a `maxLength` bump. Needs scoping decision from Krish. Progress: `qa-harness/progress/WE-20260528-888/progress.html`. |
| **Progress HTML** | `qa-harness/progress/WE-20260528-888/progress.html` |

## Description

Both checkout phone inputs use `maxLength={15}`. This was likely a guess at "phone number length" — but the actual maximum E.164 number length is 15 digits NOT INCLUDING country code prefix `+`. With spaces typically inserted by formatters:
- Indian: `+91 98765 43210` = 15 chars (just fits if no other formatting).
- UAE: `+971 50 123 4567` = 16 chars → truncated.
- UK: `+44 20 7946 0958` = 16 chars → truncated.
- US with country: `+1 415 555 2671` = 15 chars (fits).

UAE is a target market per pricing memory. Truncation = invalid phone → PayU webhook fails → user can't pay.

## Steps to reproduce

1. Open /pricing → click any paid tier → CheckoutModal opens.
2. Type a UAE phone: `+971 50 123 4567`.
3. Observe input stops at 15 chars; last digit lost; PayU rejects.

## Expected

- Use the validated PhoneInput component (src/components/auth/PhoneInput.tsx) which dynamically computes maxLength per country, OR
- Increase cap to 20 to accommodate all formatting variations, OR
- Strip non-digits before length-check (E.164 max 15 DIGITS).

## Actual

Hard cap 15 chars including spaces → international users blocked.

## Evidence

- CheckoutModal.tsx:129.
- Checkout.tsx:414.
- PhoneInput.tsx:158-208 has correct dynamic maxLength pattern.

## Notes

P1 because this directly blocks revenue (UAE, UK, etc.). Bumping from P2.

---
_Filed by `qa-visual-sprint3` on `2026-05-28T23:24:00Z`._
