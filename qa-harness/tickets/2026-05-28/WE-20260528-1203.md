# WE-20260528-1203: PostHog `message_sent` payload is PII-clean (msg_len + has_attachment) — **confirmed safe** (informational)

| Field | Value |
|---|---|
| **ID** | `WE-20260528-1203` |
| **Created** | `2026-05-28T17:36Z` |
| **Reporter** | `qa-functional` |
| **Severity** | `N/A` |
| **Category** | `edge` (privacy audit, no-regression) |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/pages/Index.tsx:436-440` |
| **URL / Page** | All chat routes |
| **Breakpoint** | `all` |
| **Status** | `info` |

## Description

PostHog payload audit, chat hot-path. `track('message_sent', { mode, msg_len: text.length, has_attachment: boolean })` — **does not** ship message content. Confirmed safe.

Same shape for `first_message_sent` (mode + time_to_first_msg_ms). Confirmed safe.

`stop_generation_clicked` ships `chars_streamed` (number) — safe.

`regenerate_clicked` — needs verification (not in this audit; see follow-up).

`message_edited` ships `msg_len`, `message_id`, `has_image` — safe.

## Notes

This ticket is informational so the next QA marathon knows hot-path chat captures have been audited and are PII-clean. Counterpart issue is **the long tail of 100+ other `track()` call sites** which were NOT audited in this pass; see -1204 for the gap.
