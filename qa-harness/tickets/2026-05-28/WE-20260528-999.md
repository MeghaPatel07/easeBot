---
id: WE-20260528-999
title: PaymentSuccess redirect — page invalidates `['account','me']` and `['account','usage']` immediately, but AuthContext.profile NOT invalidated, ProfileMenu still shows free tier
category: state-sync
severity: P0
date: 2026-05-28
component: PaymentSuccess.tsx, AuthContext, useAccount
status: open
sprint: 3
related: WE-20260528-991
---

## Steps to reproduce
1. Complete PayU checkout. Land on /payment/success.
2. PaymentSuccess fires `qc.invalidateQueries(['account','me'])` and `(['account','usage'])` per its useEffect.
3. Click "Continue", back to /.
4. Open ProfileMenu, observe plan badge.

## Expected
Badge says Pro.

## Actual
- ProfileMenu reads `useAuth().profile.plan`, not from useAccount. AuthContext.profile is loaded once and never updated by query invalidation.
- Badge says Free until reload.
- Webhook may not have written to Firestore yet (delay), but even after it does, AuthContext will not pick it up.

## Severity rationale
P0, direct customer-facing post-payment confusion.

## Suggested owner
fix-state-data
