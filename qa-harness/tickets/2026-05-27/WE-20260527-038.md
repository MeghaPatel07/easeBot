# WE-20260527-038: "Show prices in" dropdown lists currencies twice on mobile due to duplicate render

| Field | Value |
|---|---|
| **ID** | `WE-20260527-038` |
| **Created** | `2026-05-27T16:07:00Z` |
| **Reporter** | `qa-functional-3` |
| **Severity** | `P3` |
| **Priority** | Low |
| **Category** | `responsive` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/pages/Pricing.tsx` |
| **URL / Page** | `http://localhost:8081/pricing` |
| **Breakpoint** | tablet/mobile suspected |
| **Status** | `in_review` |
| **Assigned** | `fix-frontend` |
| **PR** | https://github.com/MeghaPatel07/easeBot/pull/93 |
| **Progress** | `qa-harness/progress/WE-20260527-029/progress.html` |
| **Resolution** | not-a-bug (single render verified in source) |

## Description

The pricing page body dump enumerates the eight currency codes (USD/INR/GBP/EUR/AED/SGD/AUD/CAD) only once, but the body also shows two unrelated currency lists ("Show prices in" + the live currency in the "Billed in" badges). Confirm via DOM probe whether currency buttons appear twice on tablet/mobile (rare common bug when the same toggle is mounted in both header + collapsed mobile menu).

## Steps to reproduce

1. `http://localhost:8081/pricing` at 375 px width.
2. Console: `[...document.querySelectorAll('button')].filter(b => /^USD$/.test(b.innerText)).length`.

## Expected

Returns `1`.

## Actual

To be confirmed via live probe — but the body dump suggests no duplicate; this is a low-confidence "verify" item.

## Evidence

- `qa-pages.mjs` partial body.

## Notes

If single render, close as not-a-bug.

---

_Filed by `qa-functional-3` on `2026-05-27T16:07:00Z`._
