---
id: WE-20260527-163
title: profile.avatarUrl / photo — no avatar update path; ProfileIcon avatar fallback never refreshes from Firestore
category: state-sync
severity: P1
date: 2026-05-27
component: ChatHeader.ProfileIcon / ProfileMenu / AccountTab
status: triaged
assigned: fix-state-data
---

## Steps to reproduce
1. (If avatar upload is wired) Settings → Account → change avatar.
2. Watch ChatHeader.ProfileIcon avatar, ProfileMenu avatar, ChatSidebar bottom-left avatar.

## Expected
All update within ~1s.

## Actual
- `ChatHeader.ProfileIcon` (line 87) hardcodes `<AvatarImage src="" alt="Profile" />` — fallback only.
- ProfileMenu line 171 only ever renders `AvatarFallback` initials — no image.
- AccountTab line 466 uses `getInitials` — also fallback-only.
- I.e., the codebase appears to not yet implement avatar images. If/when added, ALL three surfaces will need to be wired to the same source AND that source must be reactive (otherwise same stale-AuthContext problem).

## Impact
P1 — feature gap + future trap. When avatar upload ships, the stale-AuthContext bug guarantees the avatar UX will fail unless this is fixed first.

## Fix direction
1) Confirm with Krish whether avatar upload is in scope.
2) Fix AuthContext stale-profile FIRST (WE-20260527-150). Then wire `<AvatarImage>` consumers to `profile.photoURL` or equivalent.

## Evidence
- src/components/chat/ChatHeader.tsx:87
- src/components/ProfileMenu.tsx:170-174 (no AvatarImage)
- src/pages/settings/tabs/AccountTab.tsx:466,487

## Note (2026-05-27)
Foundation prerequisite closed by WE-20260527-170 PR — verify after merge. `AuthContext.profile` is now reactive, so once avatar upload IS wired, `profile.photoURL` (or equivalent) will fan out correctly. The feature gap (no AvatarImage consumers yet) remains a separate scoping decision for Krish.
