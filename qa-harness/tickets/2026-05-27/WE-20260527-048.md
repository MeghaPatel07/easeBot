# WE-20260527-048: No documented voice-input UI on `/` — voice mic icon appears only on mobile chat view

| Field | Value |
|---|---|
| **ID** | `WE-20260527-048` |
| **Created** | `2026-05-27T16:17:00Z` |
| **Reporter** | `qa-functional-3` |
| **Severity** | `P2` |
| **Priority** | High |
| **Category** | `functional` |
| **Repo** | `Wedding-Ease-Viva-Chat` |
| **Path** | `src/components/chat/ChatInput.tsx` (mic-button gating) |
| **URL / Page** | `http://localhost:8081/` desktop |
| **Breakpoint** | desktop |
| **Status** | `triaged` |
| **Assigned** | `fix-frontend` |

## Description

Per PRD-TTS-Pipeline + walkthroughpoints.md, voice input is a documented core feature using the Microsoft Speech SDK (Azure Cognitive Services). On the mobile home page (`qa-harness/evidence/WE-20260527-005/screenshots/mobile-home.png`) a mic icon is clearly visible in the input row. On desktop, no mic icon appears (see WE-20260527-010). Cannot test STT transcription end-to-end because the trigger UI doesn't render on desktop.

This is the spec-vs-implementation gap, separate from WE-20260527-010 (a11y / missing aria). 048 is the functional gap.

## Steps to reproduce

1. Desktop 1440×900 → `http://localhost:8081/`.
2. Look for any voice/dictate affordance in the chat composer.

## Expected

Desktop has a microphone button (with aria-label) that opens recording on press; UI shows recording duration; release/stop sends audio to `/api/transcribe`.

## Actual

No desktop UI surface. Functional coverage cannot test STT.

## Evidence

- Mobile screenshot shows mic; desktop screenshot does not.

## Notes

Coverage gap that blocks E2E voice testing for the harness.

---

_Filed by `qa-functional-3` on `2026-05-27T16:17:00Z`._
